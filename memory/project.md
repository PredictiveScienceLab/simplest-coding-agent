# Project

- Goal: teach a minimal coding agent to an engineering audience in Jupyter.
- Environment: Python with `uv`.
- Constraints: keep dependencies minimal, prefer standard library, keep the example easy to inspect.
- API scope: only `OPENAI_API_KEY`; no embedded secrets.
