[project]
name = "mini-coding-agent"
version = "0.1.0"
description = "A notebook-first walkthrough of the simplest coding agent."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.9.4",
]
